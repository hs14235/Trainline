# Native React development only. Docker Compose injects the equivalent build arg.
REACT_APP_API_BASE=http://localhost:8000
# Optional, non-secret label shown on the Engineering page.
REACT_APP_BUILD_VERSION=local-dev
