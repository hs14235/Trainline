# Convenience entry point for native development from the repository root.
# The backend copy remains canonical because the backend Docker build context
# cannot read files above backend/.
-r backend/requirements.txt
